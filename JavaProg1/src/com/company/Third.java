
package com.company;

/*
import java.util.ArrayList;
import java.util.Scanner;

public class Third
{
    public static void main(String args[])
    {
        Scanner sc = new Scanner(System.in);
        ArrayList<Integer> arr = new ArrayList<Integer>();

        // Entering Data from user into array
        first.fill_array(arr,sc);

        System.out.println("size: "+ arr.size());
        int MAX = 99999;

        boolean rec_found = false;
        int rec;

        boolean n_rec_found = false;
        int n_rec;

        for(int i =0; i< arr.size() ; i++)
        {
            int j = i+1;

            while(j< arr.size() )
            {
                if(arr.get(i) == arr.get(j) )
                {
                    rec_found =true;
                    break;
                }
                j = j +1;
            }

            if(rec_found)
            {

                System.out.println("First Recurring found: " + arr.get(i));
            }

        }

        for(int i =0; i< arr.size() ; i++)
        {
            int j = i+1;

            while(j< arr.size() )
            {
                if(arr.get(i) == arr.get(j) )
                {
                    rec_found =true;
                    break;
                }
                j = j +1;
            }

            if(rec_found)
            {

                System.out.println("First Recurring found: " + arr.get(i));
            }

        }
}
*/